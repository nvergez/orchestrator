# Secrets — never committed. Real file lives outside the repo: /home/dev/projects/orchestrator/.env
.env
.env.*
!.env.example

node_modules/

# Build output — emitted by `npm run build` (tsconfig.build.json), shipped in the npm tarball only.
dist/
